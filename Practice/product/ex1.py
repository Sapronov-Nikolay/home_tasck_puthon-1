from product import Product

# Создаём экземпляры класса Product
my_product = Product("Laptop", 1200) # Пример создания продукта

# Вызываемся
print(my_product.get_name())
print(my_product.get_price())
print(my_product.get_product_info())