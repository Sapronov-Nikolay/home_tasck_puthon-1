=== Сначала материал получен из GitHub ===
Обнаруженные дефекты:
    1. Ошибка в целевом файле слове "Пример: `capitilize" → правильно capitalize в строке 10

    2. Метод capitalize() в Python делает заглавной только ПЕРВУЮ букву строки,
       а все остальные буквы приводит к нижнему регистру.
       ("skypro", "SkyPro"),            → должно быть ("skypro", "Skypro")
       ("hello world", "Hello World"),  → должно быть ("hello world", "Hello world")

    3. В файле данном на тестирование → это целевой файл который мы тестируем при помощи тест-файла
       Сказано, что trim принимает на вход тест и удаляет пробелы в начале.
       Ничего про None не сказано!
       Технически это не баг, так как метод trim и не обещал работать с None
       Но с точки зрения надёжности программы - это уязвимость.
       Если разработчик случайно передаст None, программа внезапно упадёт.
       "Никогда такого не было, и вот опять!"
       Как тестировщик, я должен проверить "нежданчики".
       При тестировании метода trim я обнаружил, что если передать в него None, он падает с ошибкой AttributeError.
       FAILED test_string_utils.py::test_trim_negative[None-] - AttributeError: 'NoneType' object has no attribute 'startswith'
       Поэтому мы в def test_trim_negative_none() мы говорим программе что такое может быть по этому не заваливайся, а иди дальше.